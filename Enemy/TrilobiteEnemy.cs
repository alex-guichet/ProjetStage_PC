using UnityEngine;

public class TrilobiteEnemy : EnemyBase
{
    
/*
    private void Update()
    {
        if( !agent )
            return;

        if ( targetOverrideForTesting )
        {
            agent.SetDestination(targetOverrideForTesting.transform.position);
        }
        else if(isBaited)
        {
            agent.SetDestination(baitPos);
            baitTimeLeft -= Time.deltaTime;
            if(baitTimeLeft <= 0)
            {
                isBaited = false;
            }
        }
        else
        {
            if (accumulatedPlayerAggro >= minAggroToTargetPlayer)
            {
                agent.SetDestination(rover.position);
            }
            else
            {
                if( pod )
                {
                    agent.SetDestination( pod.position );
                }
            }
        }
    }
    */

}
